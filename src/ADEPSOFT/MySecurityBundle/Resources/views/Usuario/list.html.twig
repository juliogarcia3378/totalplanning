{% extends '@Comun/architecture/components/CRUD/list.html.twig' %}
{% block form_block %}
    {{ parent() }}
<div class="modal fade "  data-backdrop="static" data-keyboard="false" id="{{ model.tableId }}_modal_permisos" role="basic" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content ajax_form_block">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                <h4 class="modal-title">Adicionar {{ model.editTitle|lower }}</h4>
            </div>
            <form  action="{{path(rutas.permisos) }}"  id="{{ model.tableId }}_gestionar_permisos_form" method="POST" class="ajax_form">
                <div class="modal-body">
                    <div id="{{ model.tableId }}_gestionar_permisos_form_container" >
                        {#{{ render(path(rutas.new))}}#}
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn default" data-dismiss="modal">Cerrar</button>
                    <button class="btn blue " type="submit"  value="Aceptar">Aceptar</button>
                </div>
                <input type="hidden" name="modified" value="false"/>
            </form>
        </div>
        <!-- /.modal-content -->
    </div>
</div>
    <div class="modal fade "  data-backdrop="static" data-keyboard="false" id="{{ model.tableId }}_modal_roles" role="basic" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content ajax_form_block">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                    <h4 class="modal-title">Adicionar {{ model.editTitle|lower }}</h4>
                </div>
                <form  action="{{path(rutas.roles) }}"  id="{{ model.tableId }}_gestionar_roles_form" method="POST" class="ajax_form">
                    <div class="modal-body">
                        <div id="{{ model.tableId }}_gestionar_roles_form_container" >
                            {#{{ render(path(rutas.new))}}#}
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn default" data-dismiss="modal">Cerrar</button>
                        <button class="btn blue " type="submit"  value="Aceptar">Aceptar</button>
                    </div>
                    <input type="hidden" name="modified" value="false"/>
                </form>
            </div>
            <!-- /.modal-content -->
        </div>
    </div>
    <div class="modal fade "  data-backdrop="static" data-keyboard="false" id="{{ model.tableId }}_modal_establecer_pass" role="basic" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content ajax_form_block">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                    <h4 class="modal-title">Establecer contraseña para: <b></b></h4>
                </div>
                <form  action="{{path(rutas.establecerPass) }}"  id="{{ model.tableId }}_establecer_pass_form" method="POST" class="ajax_form">
                    <div class="modal-body">
                        <div id="{{ model.tableId }}_establecer_pass_form_container" >
                            {#{{ render(path(rutas.new))}}#}
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn default" data-dismiss="modal">Cerrar</button>
                        <button class="btn blue " type="submit"  value="Aceptar">Aceptar</button>
                    </div>
                    <input type="hidden" name="modified" value="false"/>
                </form>
            </div>
            <!-- /.modal-content -->
        </div>
    </div>
{% endblock %}